<div class="character-info">
    <h2>{{name}}</h2>
    <p>Level: {{level}}</p>
    <p>Race: {{race}}</p>
    <p>Class: {{class}}</p>
    <!-- Add more character information fields as needed -->
</div>
